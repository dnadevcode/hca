function [hcaStruct] = HCA_DBM(sets, hcaStruct)
    % HCA_DBM
    % Used for extracting kymographs from movie structure (basically copies AB_Gui/DBM_Gui commands)
    %
    %     Args:
    %         sets (struct): Input settings to the method
    %         hcaStruct (struct): Input structure, if non-empty, load
    %         result structure instead of computing everything
    %
    %     Returns:
    %         hcaStruct: Return structure
    %
    %     Example:
    %         This is an example: run [hcaStruct] = HCA_DBM(sets)
    %

end
